#include <stdio.h>
#include <stdlib.h>

int main(void)
{
    // This string is NOT one of the possible answers.
    // It is only for reference to see what a string will look like
    const char solution[] = {"THE ANSWER IS JULY"};


    // Implement your milestone logic here to determine correct solution.

    // Print out your solution like this:
     printf("%s\n", solution);

     return 0;
}